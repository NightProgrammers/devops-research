apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "statusreconciler.fullname" . }}-test-connection"
  labels:
    {{- include "statusreconciler.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "statusreconciler.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
