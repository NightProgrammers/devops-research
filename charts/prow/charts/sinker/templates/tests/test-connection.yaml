apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sinker.fullname" . }}-test-connection"
  labels:
    {{- include "sinker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sinker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
