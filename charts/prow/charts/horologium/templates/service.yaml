apiVersion: v1
kind: Service
metadata:
  name: {{ include "horologium.fullname" . }}
  labels:
    {{- include "horologium.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "horologium.selectorLabels" . | nindent 4 }}
