apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "horologium.fullname" . }}-test-connection"
  labels:
    {{- include "horologium.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "horologium.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
