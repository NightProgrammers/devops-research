apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "deck.fullname" . }}-test-connection"
  labels:
    {{- include "deck.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "deck.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
