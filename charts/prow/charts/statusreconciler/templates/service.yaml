apiVersion: v1
kind: Service
metadata:
  name: {{ include "statusreconciler.fullname" . }}
  labels:
    {{- include "statusreconciler.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "statusreconciler.selectorLabels" . | nindent 4 }}
