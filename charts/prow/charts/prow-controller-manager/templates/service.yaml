apiVersion: v1
kind: Service
metadata:
  name: {{ include "prow-controller-manager.fullname" . }}
  labels:
    {{- include "prow-controller-manager.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prow-controller-manager.selectorLabels" . | nindent 4 }}
